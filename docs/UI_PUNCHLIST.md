# Frontend / UX punchlist

Owner-reported UI/UX/deploy polish, tracked here as the durable record (the in-session task list is
ephemeral). Resilience/correctness gaps live separately in [`KNOWN_ISSUES.md`](KNOWN_ISSUES.md); the
sprint roadmap is [`PRODUCTIZING_PLAN.md`](PRODUCTIZING_PLAN.md).

Status: 🔴 open · 🟡 in progress · 🟢 done (kept for history).

## Open / in progress

| # | St | Item |
|---|----|------|
| U-11 | 🔴 | **`cortex -h` is a wall of text (CLI help summary/detail split).** Every subcommand's clap doc comment is a single paragraph, so clap uses the *whole* multi-sentence description as the short `about` — `cortex -h` and `--help` render identically, dumping ~27 paragraphs that are hard to scan. The idiomatic fix: give each verbose subcommand a concise **first-line summary**, a blank `///` line, then the existing detail — so `cortex -h` lists one-liners while `cortex <cmd> --help` keeps the rich cross-surface-twin detail. Deferred as a deliberate pass (the summary wording is a UX choice, ~19 commands) rather than churned piecemeal. (Partial: `export-dataset`'s help was brought in line with the others' "CLI twin of …" pattern, 2026-06-17.) `bin/cortex.rs` Command enum. |
| U-1 | 🟡 | **DRY the tabular reporting.** Many report tables (jobs/audit/runs/sessions/services/reports) used ad-hoc, divergent scaffolds (the `.row`/`.col-md-*` Bootstrap classes are undefined no-ops). **VISUAL/CSS DRY DONE:** spacing + type design tokens in `:root`, `.gap-*` rewired to them, `.table { margin-inline: auto }` centring, and a canonical **85% centered content column** (owner's pick) on `.report-layout` + the transitional `.col-md-10` alias (empty `.col-md-1` spacers hidden) — every tabular page now shares the same layout. **Form-screen migration DONE (2026-06-16):** the create/form family — `add-service`, `corpora-new`, `export-dataset`, `service-activate` — moved off the **no-op** `.col-md-2/8` wrapper (which rendered them full-width-left) to the canonical `.report-layout` (85% centred), and their error flash unified to `.flash-error` (icon + accent) — so every admin form now matches the report/jobs layout. **Remaining (source hygiene, deferred):** the 3 non-form no-op users (`admin`, `runs-diff`, `settings`); extract a shared page-header/table Tera partial; drain the static `style="width:N%"` column widths in history/document-report. |
| U-7 | 🟢 | **Drop "view as JSON" from public pages** (owner: "not for people to view") — DONE. Removed from the report ladder (report/severity/category) and the public overview (`/`). Admin-gated screens keep their JSON twin (intended discoverability). |
| U-3 | 🟡 | **Edge Anubis scope (owner directive).** Exempt ONLY `/healthz` (+ page assets) from Anubis; guard HTML **and** `/api/*` **and** `/entry/*` (don't let bots crawl the API or archives). Agents use **localhost** (unguarded). **Config DONE** in `deploy/edge/corpora.caddy` + `deploy/README.md` + `KNOWN_ISSUES.md` X-4 → 🟡. **Remaining (owner, on the Vultr edge VM):** append the updated block to the edge `/etc/caddy/Caddyfile` and `caddy validate && systemctl reload caddy`. Then X-4 → 🟢. |
| U-4 | 🟢 | **Progress-bar inline widths → CSS custom properties — DONE.** The data-driven bars now pass `style="--bar-width: {{pct}}%"` with `.bar { width: var(--bar-width, 0) }` in cortex.css (report/severity/category templates). Only the data value stays inline (the idiomatic custom-property pattern). **Follow-up (fold into U-1):** static `style="width:N%"` table-column widths remain in `history.html.tera` (`<th>`) + `document-report.html.tera` (`<col>`) — drain into the shared table scaffold. |
| U-5 | 🟢 | **`cortex deploy` helper — DONE.** `scripts/deploy.sh` codifies the recurring deploy: build release → migrate online (`cortex init`, backward-compatible so zero-downtime) → restart → verify `/healthz`. `--no-migrate` for code/CSS-only deploys. (A script, not a CLI subcommand — a binary can't cleanly rebuild+restart itself.) |
| U-6 | 🟡 | **Tracked `.env` carries a real DB password** (secret-in-repo, pre-existing). **Repo-side DONE (2026-06-16):** `.env` is now **untracked** (`git rm --cached`, working copy kept) + **gitignored**, with a committed `.env.example` placeholder template; production already uses the systemd `EnvironmentFile`, not the repo `.env`, so nothing breaks. **Owner action still required:** the old password is in git **history** (untracking doesn't scrub it) — **rotate the `cortex` DB password** to fully invalidate the exposed credential. Until rotated this stays 🟡. |

| U-8 | 🟢 | **Corpus-actions form — rigid aligned grid — DONE** (deployed + verified live as admin). The "Corpus actions" panel (`templates/services.html.tera`) is now one carded `<fieldset class="corpus-actions">` whose every action (Register / Extend / Carve sandbox / Delete) is a **rigid `label : control` grid row** — a fixed 12rem label column + flexible control column, so all rows align regardless of the `.center` page wrapper. Buttons use the admin vocabulary (`.btn-default` / `.btn-primary` / `.danger`), the sandbox is a sub-section (`.action-subhead` spanning both columns), Delete is set apart with a danger accent (`.action-danger`), and it collapses to stacked label-above-control under 560px. The dead `.action-panel` inline-block CSS was dropped. (Not nested cards — the rejected approach.) |
| U-9 | 🟢 | **Sandbox carve: optional size cap + entry pattern filter — DONE** (deployed; carve logic pinned by `tests/corpora_test.rs::sandbox_size_cap_and_entry_filter_limit_the_carve`). `SandboxSelection` gained `entry: Option<String>` + `max_entries: Option<i64>` (both `#[serde(default)]` so old `selection` JSON still loads); `create_sandbox` builds an always-bound `t.entry LIKE $N` (default `%`) into all 3 SQL branches and an inlined `ORDER BY t.entry LIMIT n` (validated positive i64) — server-side `INSERT … SELECT` unchanged. Wired through all three surfaces: agent `POST /api/corpora/<parent>/sandbox` (`SandboxRequest` schema), `cortex sandbox --entry --max-entries`, and the web form (U-8). Use case verified: `2506.` → `%2506.%` carves one arXiv month; the cap is deterministic (first N by entry order). |
| U-10 | 🟢 | **Report-page run-control button row — DONE** (744ad91, deployed + verified live as admin). **Pause run · Resume run · Rerun** is now the first row under the report table (`report.html.tera`, admin-only, `.footer-action` pill style); Rerun moved **out of the footer** into the row — the footer's own Rerun is suppressed on this page via a new `{% block footer_rerun %}` override in `layout.html.tera` (severity/category sub-pages keep the footer Rerun). **"Save snapshot" disabled while any task is in progress** (`global.todo`/`global.queued` → an inert `.footer-action-disabled` span, no trigger); the authoritative guard is a **409 Conflict** in both `concerns::serve_savetasks` (human) and `corpora::snapshot_tasks` (agent twin) when `status >= 0`. Renamed **"Run history" → "History"**. Verified live: row present, exactly one Rerun (footer copy gone), disabled-save span renders on an in-progress report. |

## Done (this session)

- 🟢 **Production cutover** — `cortex_load` renamed → `cortex` (main DB), `productize-2026` deployed (Arm 3 migrations applied online, zero downtime), owner-verified. See the deployment memory.
- 🟢 **Entry downloads named by document** — `/entry/import/39135` → `0811.0417.zip` (`Content-Disposition` from `helpers::entry_document_name`). Deployed + verified live.
- 🟢 **Sandbox name-collision → friendly error** — the human "Prepare a sandbox" form re-shows the corpus page with a red `.flash-error` ("A corpus named “X” already exists — choose a different sandbox name") instead of a bare 409 page (PRG via `?sandbox_taken=`), mirroring the import form's courtesy. The agent twin keeps its 409. Pinned by `corpora_test::sandbox_name_collision_reshows_a_friendly_error`.
- 🟢 **Sandbox provenance surfaced** (both surfaces, detail + list) — a carved sandbox stored `parent_corpus_id` + `selection` but nothing showed it. **Detail:** the corpus page shows a `.sandbox-provenance` accent pill ("Sandbox carved from <parent-link> · <filter>") and `GET /api/corpora/<name>` gains `CorpusDetailDto.sandbox` (`{parent, filter, selection}`). **List:** the overview badges sandbox corpora ("sandbox" pill + "carved from <parent>") and `GET /api/corpora` / `cortex corpora` gain `CorpusDto.parent` (parent name, `null` for ordinary corpora) — so a caller tells sandboxes apart from the list alone, no per-corpus fetch. The filter summary is DRY'd into `SandboxSelection::filter_summary()` (one source of truth). Pinned by `corpora_test` (provenance + parent present on a carve; `null`/absent on a plain corpus).
- 🟢 **Brighter light-theme warning bar** (744ad91) — `.bar.warn` now uses a bar-only `--bar-warn` (`#ca8119`, owner's pick) instead of the WCAG-darkened text `--warn` (a block fill needs no text-contrast darkening); midnight keeps `#f5b042`.
- 🟢 **/jobs table centering** — `.table { margin-inline: auto }` (c58b848).
- 🟢 **Admin stat-card widening** — large corpus counts no longer break mid-number (2b2f67d).
- 🟢 **U-2: API-docs nav summaries** — short per-operation summary derived in `apidoc::mount` so the RapiDoc left-nav is readable (7c15f7c).
- 🟢 **U-7: "view as JSON" off the public report ladder** — removed from report/severity/category (public overview flagged in U-7 above).
- 🟢 **U-4: progress-bar widths → `--bar-width` custom property** (c698c11, deployed).
- 🟢 **U-5: `scripts/deploy.sh`** — the recurring deploy (build → migrate online → restart → verify).
- 🟢 **"Look up an article" fixes** (4882e6f, deployed): real placeholder example (`1308.3966`), and the human `/document/…/<name>` renders a friendly "no such article + back to report" page on a miss instead of the bare 404 catcher (agent twin keeps its plain 404).
- 🟢 **Forensics info-messages accordion** (672d32a, deployed): the collapsed `<details>` summary is now link-coloured + bold with a large rotating ▸ chevron, so it reads as expandable.
