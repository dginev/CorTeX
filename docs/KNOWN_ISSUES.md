# CorTeX Known Issues — resilience & correctness ledger

> Running list of known problems, weighted toward **robustness/fault-tolerance** gaps (see
> [`DESIGN_PRINCIPLES.md`](DESIGN_PRINCIPLES.md)). Owner's direction: **record every known problem as
> we find it; we go back and solve them all at the end.** Do not silently fix-and-forget, and do not
> silently leave a discovered gap unrecorded.
>
> Status legend: 🔴 open · 🟢 resolved (kept for history). This ledger is **bugs only**; mitigated
> items with a documented upgrade path (the former 🟡 "partially mitigated" set) live in
> [`POSSIBLE_UPGRADES.md`](POSSIBLE_UPGRADES.md) — stretch goals, not defects.
> Severity: **S1** can crash/corrupt/destabilise the system · **S2** drops or hides work ·
> **S3** correctness/UX papercut · **S4** cleanup/polish.

> **2026-06-16 — clean slate.** Every bug tracked through the productize-2026 sprint reached 🟢
> (36 of 36); that all-resolved ledger is archived verbatim at
> [`archive/KNOWN_ISSUES_June16.md`](archive/KNOWN_ISSUES_June16.md). The ledger reset clean; new
> findings are recorded under **Open issues** below the moment they're discovered, with a stable ID,
> severity, and a one-line fix direction; promote 🔴→🟢 (don't delete) when fixed, with the commit
> that did it.

| # | Sev | Status | Issue |
|---|---|---|---|
| D-19 | S3 | 🔴 | **The lease reaper is worker-request-gated, so it goes quiescent when the fleet idles — expired in-flight tasks (notably D-18-deferred empty results) aren't reaped until activity resumes or a dispatcher restart sweeps them (found 2026-06-21).** `reap_expired_into` is invoked only from the ventilator's request-handling path (`ventilator.rs:207,215` — `request_time = Utc::now()` *after a request arrives*, then `if request_time - last_reap >= reap_interval`), and the D-15 `RCVTIMEO` loop `continue`s on the idle `EAGAIN` **before** reaching that check — so the reap cadence is driven by **worker requests, not the wall clock**. When a run drains all new `TODO` and only in-flight work remains — e.g. D-18-deferred empties awaiting retry — the workers fall idle / back off / exit, requests dry up, and the reap stops firing: the expired leases sit indefinitely (status = a positive lease mark) instead of being requeued/dead-lettered. Observed in the D-18 verification (2026-06-21): after ~357 transient empties recovered and 14 dead-lettered, the final **23** deferred empties froze in-flight at one lease mark with the reaper quiescent (0 reaps for >6 min while workers logged "waiting for jobs"), until a dispatcher restart's `clear_limbo_tasks_except` reset them to `TODO`. **No data loss** (in-flight is recoverable; the restart limbo-sweep recovers them) and **benign at scale** (a 3M-task run keeps the fleet continuously busy → the reaper fires constantly → deferred empties resolve mid-run); it manifests only at an idle tail — but it is a liveness gap: **D-18's retry/dead-letter guarantee silently depends on continued fleet activity.** **Fix:** also run the reap on the idle `EAGAIN` tick (wall-clock `now()` vs `last_reap`) so it fires every `reap_interval` regardless of requests; then idle in-flight tasks reap + retry/dead-letter promptly. D-16-adjacent (both are dispatcher idle-behaviour gaps). `src/dispatcher/ventilator.rs` (reap trigger / the `EAGAIN` loop-back), `src/dispatcher/server.rs::reap_expired_into`. |
| D-14 | S2 | 🔴 | **A result for a task deleted while in-flight poisons its whole finalize batch (→ dispatcher restart + re-converted work).** `mark::mark_done` persists each finalize drain **batch in one transaction**: it batch-INSERTs `log_*` rows keyed by `report.task.id`. If a task is deleted while its worker is still converting it (an admin deletes/clears a corpus or service mid-run → the FK cascade removes the task), the returning result still references that `task_id`, so the `log_*` INSERT **FK-violates** (`log_*.task_id → tasks(id)`; `ON DELETE CASCADE` covers deletes, not a fresh insert). One violation rolls back the **entire** transaction → `mark_done_batch`'s retry re-hits it (the task is still gone) → `Err` propagates → the dispatcher fail-fasts and restarts, and every **valid** result in that batch is rolled back and **re-converted** after the restart. Blast radius not isolated (one stale task wastes its whole batch), and a *recoverable* condition (a vanished task — nothing to persist) triggers a heavy restart instead of "log, count, continue" (DESIGN_PRINCIPLES). Self-healing (restart recovers; no data loss), so not urgent — but wasteful + disruptive on a reachable race (deleting an actively-converting corpus). **Fix plan (low-risk, hot path untouched):** on the **retry path only** (the common case stays the fast single-transaction insert), filter the reports to task ids that still exist — `SELECT id FROM tasks WHERE id = ANY(batch_ids)` — drop+log the vanished ones, and retry `mark_done` with the survivors, so a deleted task is simply skipped (its work has no target) rather than restarting the dispatcher. Left for the owner: it's in the dispatcher finalize path (the active throughput-research area). Found 2026-06-17 by the delete-while-in-flight robustness lens. `src/backend/mark.rs::mark_done`, `src/dispatcher/server.rs::mark_done_batch`. |
| D-16 | S3 | 🔴 | **Dispatcher idle graceful-shutdown still hangs the full `TimeoutStopSec` → SIGKILL *despite* the D-15 ventilator fix (found 2026-06-20).** During the 2026-06-20 worker-rebuild deploy, `scripts/deploy.sh`'s `systemctl restart cortex-dispatcher` logged the O-1 graceful shutdown (`08:55:16` "draining in-flight results, then stopping"), then systemd reported `stop-sigterm timed out. Killing.` and **SIGKILLed it ~90s later** (`08:56:46`) — the exact D-15 idle-hang signature, with the fleet **offline (0 in-flight)** at the time. Crucially, the hung binary was the Jun-19 10:16 build (process uptime ~22.5h), which **postdates** the D-15 fix commit `5eb4b81` (Jun-18 19:17) and therefore **already contained** `ventilator::set_rcvtimeo(250)`. So the ventilator RCVTIMEO fix is **necessary but not sufficient**: with the ventilator now polling out every 250ms, the O-1 shutdown still blocks elsewhere — the **sink receive loop and/or the finalize-thread join**, which wait on a blocked `recv`/`join` with no shutdown-flag re-check when there is nothing to drain (per `sink.rs`, the bounded `recv` only re-checks in `job_limit = Some(_)` mode; the unbounded production sink loop + the finalize join are the suspects). Impact = identical to D-15: every dispatcher restart/deploy costs ~90s + SIGKILL + a dropped finalize batch (reaper recovers → no data loss), benign on a one-off deploy but **wasteful + disruptive at 3M scale** where deploys/restarts recur across a multi-day run. **Not yet verified on the current HEAD binary** (needs a controlled idle `systemctl restart`), but the shutdown path is unchanged from the hung binary, so it very likely repeats. **Fix direction:** give the sink receive loop a `RCVTIMEO` + shutdown-flag re-check (mirror the ventilator fix) and bound the finalize-thread join, so the whole O-1 shutdown observes the flag promptly with an idle fleet; then re-verify with a clean idle restart. Found 2026-06-20 during the worker-rebuild deploy. `src/dispatcher/sink.rs` (receive loop), `src/dispatcher/finalize.rs` (join), `src/dispatcher/server.rs` (O-1 coordination). |
| R-13 | S3 | 🟢 | **`mark_rerun`'s temporary task sentinel overlapped the live-lease value space — a concurrent rerun could double-dispatch an in-flight task (found + fixed 2026-06-20).** Found by the rerun clean-slate audit. The log-deletion itself is correct on **every** rerun surface (human routes, agent `POST /api/reports/<c>/<s>/rerun`, `cortex rerun`): all funnel through `mark_rerun`, which for **every** task it resets deletes **all five** `log_*` tables (not just the filtered severity), and `mark_done` re-clears on completion — so a re-converted task always starts clean. The gap was the *sentinel's value space*, not the deletion. `mark_rerun` does a two-phase reset: stamp the in-scope tasks with a temporary `mark`, re-select them by `status = mark` to delete their logs + flip to `TODO`. The mark was `random_mark()` — a raw `u16` cast to `i32`, range **`[0, 65535]`** — but the ventilator's live lease mark (`tasks_aggregate::fetch_tasks`) is `1 + u16` → **`[1, 65536]`**, which **overlaps** it. So if a rerun's random sentinel equalled the lease value of a task currently *in-flight* in the same `(corpus, service)`, that unrelated task was swept into `affected_tasks` → reset to `TODO` mid-flight → **double-dispatched** (re-converted when its original result lands; `mark_done` recovers it, so no data loss, but wasted compute + a transient phantom status). Probability ≈ in-flight-set-size / 65536 — non-negligible on a busy corpus (hundreds in flight). Separately, `random_mark()` could return **`0`**, aliasing `TODO`, so the re-select `status = 0` matched every already-`TODO` task in the pair (benign — they carry no logs — but the two-phase machinery degenerates). Same class as the D-4 limbo double-dispatch. **Fixed:** a dedicated `helpers::rerun_mark()` draws a **positive** sentinel strictly *above* the max lease — `[65537, 131072]` — disjoint from leases `[1, 65536]`, `TODO` (`0`), and completed/Blocked (`< 0`); `mark_rerun` uses it (`random_mark` kept unchanged for its generic test-only use). Owner constraint honoured: all temp marks stay `> 0`. Pinned by `helpers::rerun_mark_tests::rerun_mark_stays_above_the_lease_space` (10k draws, all `> 65536`). `src/helpers.rs::rerun_mark`, `src/backend/mark.rs::mark_rerun`. |
| D-18 | S2 | 🟢 | **An empty / unreadable worker result is silently marked *terminal* `Fatal` — no DB message, no retry — so a recoverable infrastructure failure is recorded as (and indistinguishable from) a conversion verdict (found 2026-06-20).** `helpers::generate_report` defaults `status = Fatal` and only escapes it by parsing a status line out of the result's `cortex.log`. If `read_cortex_log` returns `Err` — a 0-byte / non-zip / corrupt archive, or a zip with **no `cortex.log` entry** — the Err branch only `println!`s to dispatcher stdout (**not** a `log_*` row) and the task is left `Fatal` with **zero messages**. Because a result *was* returned (the task already left the in-flight set), the reaper/retry path (`classify_expired`) never sees it, so it's **terminal on first occurrence** — unlike a timeout, which retries up to `MAX_DISPATCH_RETRIES`. Result: an infra failure (empty/garbled send) is (a) **invisible** — no message in any `log_*` table, so it never shows in reports, the run-diff, or `log_fatals` buckets (only a stdout line that, as seen this run, journald didn't even capture), and (b) **unrecovered** — the work is dropped, not re-dispatched, even though a re-convert would likely succeed. Violates the maximum-robustness mandate on two counts: *transparent failure* (silent) and *no silent loss / degrade-gracefully* (no retry of a recoverable condition). **Evidence:** the 2026-06-20 Perl `tex_to_html` run (#192) had 599 Fatal vs a 2026-04-29 baseline of ~202; the run-diff (`/api/runs/sandbox-arxiv-10k-shuffle/tex_to_html/diff`) showed 447 newly-Fatal, but **427 of the 599 carried zero `log_*` rows** and **all 427 result archives are 0-byte** (`<entry-dir>/tex_to_html.zip`), a steady ~4.3% across the run — i.e. the real conversion-Fatals (~172: `too_many_errors` 129, `timeout` 15, `perl` 13, `cortex` 12) are actually *below* baseline, and the "slippage" is entirely empty results. Engine-agnostic gap but **Perl-amplified**: the Perl `latexml_worker` self-kills a conversion that blows its 6 GB/45-min budget (`Proc::ProcessTable`; **not** kernel OOM — `journalctl -k` clean) and returns an empty archive with no `cortex.log`; the same corpus on the Rust `oxidized-tex-to-html` worker produced only **4** such empties of 51 Fatal (≈0.04% vs 4.3%, ~100×), because latexml-oxide converts those hardest papers. **Fix direction:** (1) *transparency* — `generate_report`'s Err/empty-message path should record a diagnostic `Fatal` message (e.g. `cortex / empty_result` or `unreadable_archive`) into `log_fatals` instead of only `println!`, so the failure is visible in the DB like every other Fatal; (2) *resilience* — treat a 0-byte / unreadable / `cortex.log`-less result as a **retryable** infrastructure failure (re-queue via the `classify_expired` budget) rather than a terminal verdict on first occurrence, dead-lettering with a message only after retries exhaust; keep "cortex.log present but maps to `conversion:3` with no messages" as a genuine terminal Fatal (but still record a message). Distinguishes infra noise from conversion quality and stops silently dropping recoverable work. Found 2026-06-20 by the Perl-vs-Rust Fatal-slippage run-diff. **Fixed 2026-06-20** — the minimal fix fell out of the architecture: the sink's existing *no-result-file* branch already skips finalizing and lets the reaper recover the task, so the only bug was that the *0-byte-file* path went through `generate_report` → a fabricated terminal Fatal instead. `generate_report` now returns **`Option<TaskReport>`** (`None` on a `read_cortex_log` Err = 0-byte / truncated / non-zip / no `cortex.log`), and the sink's `Commit` routes `None` to that same skip-and-recover path — the lease reaper retries via `classify_expired` and dead-letters with a visible `cortex/never_completed_with_retries` `log_fatals` message after `MAX_DISPATCH_RETRIES`. So *retry* (transient empties self-heal) **and** *transparency* (the terminal case carries a message) both land via the existing mechanism — no silent, unretried Fatal; a readable `cortex.log` still yields `Some` (a genuine verdict, even when `Fatal`). Pinned by `helpers::log_decode_tests::generate_report_defers_unreadable_archives_to_the_reaper`. **Corrected root cause:** the Perl rerun on `/opt/cortex-scratch` (2026-06-20) showed the empties are **not** `/dev/shm`/staging exhaustion — `mem_guard` fired **0×** and ~4% empties persisted on a 913 GB disk scratch (only a slight drop from 4.3%); they are the Perl `latexml_worker`'s `convert()` writing a **0-byte output** (latexmlc crash/abort, no `cortex.log`), staging-independent. So this cortex-side retry — not the scratch move — is the engine-agnostic recovery. `src/helpers.rs::generate_report`, `src/dispatcher/sink.rs` (Commit `None` branch), `examples/recover_log_reports.rs`. |
| D-15 | S3 | 🟢 | **Dispatcher graceful shutdown hung the full `TimeoutStopSec` (120s) when the fleet was idle (found + fixed 2026-06-18).** The O-1 graceful-shutdown sets a flag and waits for the ventilator thread to return, but an **idle** ventilator (no workers requesting work) is blocked in a ROUTER `recv` with nothing to wake it, so the flag is never observed until systemd's `TimeoutStopSec` (120s) elapses and it SIGKILLs the process. No data loss (in-flight tasks are re-leased on the next start; only the pending finalize batch is dropped, which the reaper recovers), but it makes `systemctl restart cortex-dispatcher` pause ~2 min when idle, and it is the root cause of the "**Address already in use**" bind crash-loop when a **non-systemd** dispatcher (e.g. a stray `scripts/run_dispatcher.sh`) is SIGTERM'd and clings to `:51695`/`:51696` during the hang (systemd-managed restarts are unaffected — systemd SIGKILLs the old before starting the new). **Fixed:** the ventilator ROUTER now sets a 250ms `RCVTIMEO` and treats the leading recv's `EAGAIN` (timeout) as "loop back", so the lease loop re-checks the shutdown flag every ~250ms — an idle dispatcher stops within ~250ms of SIGTERM (no SIGKILL wait), which also makes `scripts/deploy.sh`'s dispatcher restart snappy. Found + fixed 2026-06-18 during the dispatcher systemd cutover. `src/dispatcher/ventilator.rs` (lease loop), `src/dispatcher/server.rs` (the O-1 shutdown flag). |

## Resolved since the reset

| # | Sev | Status | Issue |
|---|---|---|---|
| F-7 | S3 | 🟢 | **Sandbox carve conflated task-status with message-severity — fixed via Model C (found 2026-06-17, fixed 2026-06-17).** The carve validated one overloaded `severity` with a flat `TaskStatus::from_key` and coupled `t.status = severity` **even for a `category`/`what` (message) narrowing** — so `info`+category was wrongly **rejected** (info has no `TaskStatus`, only `log_infos`), and a message filter was wrongly constrained to the task's own status (`no_problem` tasks that emitted a warning message were uncarvable). **Fixed (owner-confirmed Model C):** `SandboxSelection` now carries independent, **intersected** `status` (task outcome) + `message_severity` (`info`|`warning`|… via a fixed message→`log_*` map) dimensions, each optional, at least one required. The carve drops the coupling — an optional `t.status = <status>` intersected with an optional `EXISTS` over the message `log_*` table — so a message filter matches a task at **any** status (the cross-cutting carve the owner wanted; `info`+category now works). `validate()` replaces `validated_status` as the shared pre-flight; threaded across the agent API, the human form (two selects), and the CLI (`--status`/`--message-severity`); a legacy read-only `severity` field keeps pre-Model-C stored selections rendering their provenance. `sandbox.rs` unit tests pin the contract (info+category valid, info-as-status invalid, category-needs-message, legacy summary); `corpora_test` re-pointed; clippy clean. `670f559` + `0bee097`. `src/backend/sandbox.rs`, `src/frontend/corpora.rs`, `bin/cortex.rs`, `templates/services.html.tera`. |
| R-12 | S3 | 🟢 | **`register_service` (activate) recorded its run *outside* the task-creation transaction (found + fixed 2026-06-17).** R-11's sibling, found by the same crash-consistency sweep: the activate op created the conversion tasks in one transaction but called `mark_new_run` **after** it, so a `mark_new_run` error (or a crash there) left the tasks committed while `register_service` **returned `Err`** — the activate job reports *failed*, yet the tasks exist (and convert), with **no run recorded**: an inconsistent "failed activate that actually activated." **Fixed:** moved `mark_new_run` **inside** the same transaction (nests as a savepoint), so activate is all-or-nothing — a failure rolls the freshly-created tasks back too. (`extend_service` is a single atomic `INSERT`; `destroy_service` is already transactional — sweep complete.) `services_test` green. `src/backend/services_aggregate.rs::register_service`. |
| R-11 | S3 | 🟢 | **`mark_rerun` was not atomic — a frontend crash mid-rerun could strand the scope in a phantom status (found + fixed 2026-06-17).** `mark_rerun` recorded the run (`mark_new_run`) then, in **separate auto-committed statements**, set the matching tasks to a temporary `random_mark()` status, deleted their logs, and flipped `mark → TODO`. A frontend crash/restart (deploy, panic, OOM) between "set to `mark`" and "set to `TODO`" left the whole rerun scope stuck in the `mark` value — a positive status that reports read as "Queued/in-progress" but the dispatcher **won't lease** (it leases `TODO = 0`) — so those tasks didn't re-convert, showing a misleading in-progress status until a dispatcher-restart limbo recovery (`clear_limbo_tasks_except` resets positive→TODO) or a re-rerun. Self-healing eventually, but observably wrong for the interval. **Fixed:** wrapped the whole body (`mark_new_run` + the mark/delete/`TODO` statements) in **one `connection.transaction`** — the closure param shadows `connection` so the body is unchanged, and `mark_new_run`'s own transaction nests as a savepoint — so a crash rolls back the entire rerun (no phantom run, no stranded `mark`) and the temporary `mark` is never observable. Matches `mark_done`'s existing transaction pattern. `rerun_test` + `reports_api_test` green. `src/backend/mark.rs::mark_rerun`. |
| B-1 | S2 | 🟢 | **`cortex.toml` was written non-atomically — a crash mid-write corrupts the config (found + fixed 2026-06-17).** **All three** config writes — `cortex init`'s scaffold, `cortex set-admin-token`'s `[auth]` update, and the **Settings page / `PUT /api/config`** runtime persist (`management::merge_and_persist`) — did a direct `std::fs::write(config_path, …)`. A crash/kill between opening and finishing the write leaves a **truncated `cortex.toml`** that figment can't parse → the frontend/dispatcher/CLI won't start; worse, `cortex init` **skips** an existing (partial) file (`if config_path.exists()`), so the corruption isn't auto-repaired, and `set-admin-token`'s in-place rewrite would also lose the **previous** valid config. Low likelihood (a sub-millisecond setup-time write) but disruptive (a config that won't load, during install). **Fixed:** a shared `write_config_atomically` helper writes a sibling `cortex.toml.tmp` then `fs::rename`s it over the target — atomic on a POSIX FS, so a crash leaves the previous file (or none) intact, never a half-written one; `fs::write` truncates any orphaned temp. (Process-crash atomic; power-loss durability would add an `fsync`, overkill for a setup-time write.) All three callers share the one `pub(crate)` helper. Sibling of E-4 (export atomic-publish), same lens. `bootstrap_test` + `settings_test` + `management_api_test` green. `src/bootstrap.rs::write_config_atomically`, `src/frontend/management.rs`. |
| E-4 | S2 | 🟢 | **Export resume could keep a corrupt archive after a crash mid-write (found + fixed 2026-06-17).** The dataset export streams each group's papers into `<corpus>-<key>.zip` and is **resumable** by skipping any archive whose `.zip` already exists — but it wrote **directly to the final `.zip`** (`File::create(final_path)`), so a crash/kill/restart between opening the writer and `ZipWriter::finish()` (which writes the central directory) left a **truncated, corrupt `.zip`**. On resume, the final-name existence check skipped it as "done" → the dataset silently shipped a corrupt, unopenable archive. Reachable on any restart/interrupt during a long export. **Fixed:** atomic publish — each archive is written to `<name>.partial` and `fs::rename`d to `<name>.zip` only after `finish()` succeeds, so a crash leaves a `.partial` (which the resume's final-name check ignores → re-writes) and **only a complete archive ever gets the final name**; `File::create` truncates any orphaned `.partial`. No regression — `export_test` green (archives valid + resume still skips completed ones); the crash window itself isn't unit-simulable, the fix is the canonical write-temp-then-rename. `src/backend/export.rs::ArchiveStreamer`. |
| I-3 | S2 | 🟢 | **Importer directory walk was unbounded on a symlink loop (found + fixed 2026-06-17).** `Importer::walk_import` is an iterative stack walk that uses `fs::metadata` (which **follows symlinks**) to decide whether to recurse, with no depth bound and no visited-tracking. A corpus containing a **symlink loop** — malicious, or an accidental backup/snapshot link — made the walk follow it forever: unbounded distinct paths → unbounded `TODO` task creation + DB bloat, and because the job keeps finding entries it keeps *progressing*, so the heartbeat-keyed stale-reap (W-4) never fires and it never stops. Exactly the "arXiv data is hostile → no unbounded per-event resource acquisition" case (DESIGN_PRINCIPLES). **Fixed:** the walk queue now carries a depth and a path past `MAX_WALK_DEPTH = 64` is skipped (logged); arXiv-topology corpora are ~3 levels deep, so 64 bounds any loop without truncating a real corpus. Pinned by `importer_test::import_walk_terminates_on_a_symlink_loop` (a `loop → root` self-loop import **terminates** with a bounded count instead of hanging — the test would *hang*, not merely fail, without the bound). `src/importer.rs::walk_import`. |
| R-10 | S3 | 🟢 | **One all-invalid run blanked the *entire* history chart (found + fixed 2026-06-17).** `RunMetadataStack::transform` (the `/history` chart data) computed each severity bar as `100.0 * field / total`; `total` **excludes invalids**, so a run whose tasks are *all* `Invalid` (or an open run before any completion) has `total = 0` → `NaN`/`Inf` percents. `serde_json` can't encode those, and the failure is on the **whole array** — so one such run made `history_serialized` fall back to empty and blanked the *entire* chart (every run vanished, not just the bad one). Reachable on any `(corpus, service)` whose latest conversions are all `Invalid`. **Fixed:** `transform` now skips zero-total runs (no success-rate to plot), so the NaN is never produced and the remaining series charts normally. Pinned by `historical_runs::tests::transform_skips_zero_total_runs_so_one_doesnt_blank_the_chart`. (Sibling of the `aux_stats_compute_percentages` divisor-clamp that already protects `progress_report`'s percentages.) `src/models/historical_runs.rs::transform`. |
| X-12 | S2 | 🟢 | **`GET /api/services` + `/api/services/<svc>/workers` shipped ungated — public registry + worker-fleet disclosure (found + fixed 2026-06-17).** The third pair found by the read-path authz sweep (after X-10 config, X-11 jobs): both agent reads took **no guard**, while the **human** `/services` and `/workers/<service>` both `require_admin_to`. `api_service_workers` is the more sensitive — `WorkerDto.name` is the worker's **`hostname:pid`**, so an anonymous caller could enumerate the **worker-fleet topology**: hostnames, per-worker dispatch/return tallies, in-flight backlog, liveness. `api_services` exposed the full registry (every service's name/version/formats). Gating doesn't break agent discovery — the onboarding doc lists only `/api/corpora` + `/api/reports` as entry points, and a corpus's services are already in `CorpusDetailDto.services` (`GET /api/corpora/<name>`). **Fixed:** `_caller: Actor` token-guard on both (clean `401` without a token), matching their admin-only human twins. Pinned by `services_test` (no token → 401 on both; `?token=` → 200/404). `src/frontend/services.rs::{api_services,api_service_workers}`. |
| X-11 | S2 | 🟢 | **`GET /api/jobs` + `/api/jobs/<uuid>` shipped ungated — public admin-activity disclosure (found + fixed 2026-06-17).** Sibling of X-10, surfaced by continuing the read-path authz sweep: both agent job-read endpoints (`api_jobs`, `api_job`) took **no request guard**, while the **human** `/jobs` + `/jobs/<uuid>` require `require_admin_to` and `JobDto` carries `actor` (admin attribution) + `params`. So an anonymous caller could `GET /api/jobs` and read the **history of admin operations with attribution** — who ran which import/extend/activate/delete/rerun/export, on which corpus/service, to which output paths — plus job timing/status/progress. Admin-activity disclosure, analogous to the token-gated `/api/audit`; a `jobs_api_test` comment even *claimed* "the `/api/jobs` twins stay token-based" (they weren't). **Fixed:** added the `_caller: Actor` token-guard to both (clean `401` without a token), matching the human `/jobs` and the other admin-only agent reads. Pinned by `jobs_api_test` (no token → 401 even for an unknown uuid, since the guard precedes the lookup; `?token=` → 200), with the `services_test` + `management_api_test` callers updated. `src/frontend/jobs.rs::{api_jobs,api_job}`. |
| X-10 | S2 | 🟢 | **`GET /api/config` shipped ungated — public operational-config disclosure (found + fixed 2026-06-17).** The agent config-read twin `api_config` took **no request guard** (`pub fn api_config() -> Json<ConfigDto>`), so any anonymous caller could `GET /api/config` and read the config DTO. Secrets *are* masked (DB password → `***`; only the token **count**, never values), but the **operational/infrastructure config leaked**: DB host/user/dbname, dispatcher ports, pool/queue sizes, timeouts, asset paths — reconnaissance an anonymous caller shouldn't get. This is the **read-twin of X-5** (the `PUT /api/config` ungated gap): the *write* was caught + gated, the human `/settings` requires `require_admin`, and a module comment even *claimed* `/api/config` "keeps its own guard" — but the GET was missed and was in fact public. Surfaced by an exhaustive write-then-read authz sweep of every route. **Fixed:** added the `_caller: Actor` token-guard (clean `401` without a token), matching `api_health`, the `PUT` twin, and `/settings` — config is now admin-only on **every** surface. Pinned by `management_api_test` (no token → 401; `?token=token1` → 200 masked contract). `src/frontend/management.rs::api_config`. |
| X-8 | S2 | 🟢 | **Path-traversal hardening on the public archive-download route — found + fixed 2026-06-17.** `GET /entry/<service_name>/<entry_id>` (public, no auth) resolves the served file via `result_archive_path`, which interpolates the raw `<service_name>` URL segment into a **filesystem path** (`{entry_dir}/{service_name}.zip`) with **no** service-registry lookup (unlike the `serve_entry_preview` path, which does `Service::find_by_name`). A real service name is a bare identifier, but a crafted segment (`..%2f..%2f…`) decoded by Rocket to contain `../` could resolve `{entry_dir}/../../…​.zip` outside the corpus data directory — a `.zip`-scoped local-file read on a public route (the `.zip` suffix and Unix's no-NUL-in-paths bound it; exploitability also depends on Rocket's segment decoding, so this is defense-in-depth on a clear anti-pattern: unvalidated user input → fs path). **Fixed:** `serve_entry` now rejects any `service_name` containing a path separator (`/`/`\`), `..`, or NUL with a `404` **before** any `NamedFile::open`. Pinned by `reports_api_test` (a `..`/encoded-slash service segment 404s with the guard's `invalid service` body even for a real task id, proving the guard fired, not an incidental file-miss). `src/frontend/concerns.rs::serve_entry`. |
| X-7 | S2 | 🟢 | **Stored XSS in the public history chart — found + fixed 2026-06-17.** `/history/<c>/<s>` is a **public** page; its run-quality chart embeds `{{ history_serialized \| safe }}` (a serde_json array of run metadata) and `{{ global.corpus_name \| safe }}` **inside a `<script>` block**. serde_json escapes `"`/control chars but **not** `<`, so a user-set run **description** (rerun/snapshot `--description`) containing `</script>` would close the script tag and inject arbitrary markup/JS that runs for any anonymous viewer — and `corpus_name \| safe` was a second, direct vector (the adjacent `service_name` was correctly auto-escaped, making the `\| safe` an inconsistency). Injection requires an admin write, but execution is public (could hijack a viewing admin's session), so a real authenticated-stored-XSS. **Fixed:** the handler now escapes `<`/`>`/`&` in the serialized JSON to their `\uXXXX` forms (`JSON.parse` decodes them back, so the chart is byte-identical) and the `corpus_name \| safe` was dropped (auto-escaped, matching `service_name`). The only two `\| safe` uses in the templates; no other user data reaches a raw context. Pinned by `runs_test::history_chart_escapes_script_breakout_in_a_description` (a `</script>` payload renders escaped, never raw). `src/frontend/runs.rs`, `templates/vega-history.html.tera`. |
| R-9 | S3 | 🟢 | **Rerun severity validation was inconsistent across surfaces and used the wrong set — found + fixed 2026-06-16.** The agent rerun reused `is_rollup_severity` (the *report* validator, `warning\|error\|fatal\|invalid\|info`), which is wrong for *rerun*: it **rejected `no_problem`** (a legitimate rerun scope — re-convert already-clean tasks, e.g. after a worker upgrade) and **accepted `info`**, which `mark_rerun` then silently mis-scoped to `no_problem`; the human `serve_rerun` did **no** severity validation, so a typo silently reran `no_problem`. Resolved with a **rerun-specific, context-aware** validator `is_valid_rerun_severity(severity, has_category)`: *with* a category the scope is over log messages (`warning\|error\|fatal\|invalid\|info`), *without* one over task **status** (`no_problem\|warning\|error\|fatal\|invalid`) — the inherent semantics (rerun filters tasks/messages, it doesn't aggregate a report). Applied on **all three** surfaces — `rerun_report` (agent), `serve_rerun` (human), and `cortex rerun` (CLI) — so they accept/reject the same set; `mark_rerun`'s `unwrap_or(NoProblem)` / `LogInfo` fallbacks are now unreachable (kept as defense-in-depth). **(2026-06-17 follow-up:** the CLI was initially missed — `run_rerun` validated with a flat `TaskStatus::from_key` regardless of `--category`, so it wrongly rejected `info --category` and accepted `no_problem --category`; now it calls the shared `is_valid_rerun_severity` like the other two.) Pinned by `reports_api_test` (`no_problem`/no-category accepted; `info`/`bogus` rejected on the agent; `bogus` rejected on the human path), the `reports::tests::rerun_severity_is_context_aware` unit test (both branches of the shared validator), and `rerun_test` (a valid `warning` rerun still works). `src/frontend/reports.rs`, `src/frontend/concerns.rs`, `bin/cortex.rs`. |
| X-6 | S3 | 🟢 | **The web UI loaded its base CSS/JS/fonts from external CDNs — fixed (2026-06-16, recorded + resolved same day): the UI is now fully self-contained.** `layout.html.tera` pulled 5 resources from 3 third-party hosts (bootstrap 3.3.1 CSS+JS + font-awesome 4.4.0 from the deprecated `maxcdn.bootstrapcdn.com`, jQuery 2.1.4 from `code.jquery.com`, Roboto Slab from `fonts.googleapis.com`) — the one part of an otherwise self-installing app that wasn't self-contained (broken layout/icons if a CDN dies, external DNS/TLS round-trips, every visitor disclosed to third parties, all bypassing the operator's edge wall). **Vendored** into `public/vendor/{css,js,fonts}` and the layout repointed at local copies: jQuery + bootstrap `.min.{css,js}`; font-awesome `.min.css` **with its `fontawesome-webfont.{woff2,woff,ttf}`** at the CSS-relative `../fonts/` (icons are used); and Roboto Slab self-hosted (latin-subset `woff2` for 400/700 + a local `@font-face` with `font-display: swap`, falling back to the `--serif` system stack). Now **zero external requests** — verified on a dev frontend: all 8 assets return `200` with correct content-types and the rendered page has 5 local refs and no CDN refs. Self-contained, faster, private; deployed + re-verified on prod. `templates/layout.html.tera`, `public/vendor/`. |
| I-2 | S4 | 🟢 | **`activate`/`extend` loaded a corpus's whole import-entry list into RAM — fixed (2026-06-16, recorded + resolved same day).** `services_aggregate.rs` (`Backend::activate`/`extend`, reached from the web `/services/<s>/activate` + the agent `POST /api/corpora/<c>/services/<s>` and `…/extend`) loaded **every** import `entry` of a corpus into a `Vec` (~1.5M strings ≈ 120 MB for arxmliv), then issued one `create`/`create_if_new` per entry — O(corpus) RAM **and** ~1.5M per-document round-trips. Both now do the task creation as **one server-side `INSERT … SELECT`** straight from the `import`-service rows (`extend` adds `ON CONFLICT (entry, service_id, corpus_id) DO NOTHING` — the bulk twin of the old per-entry `create_if_new`, preserving its only-new semantics; `activate` is guarded against an already-registered pair so needs no conflict clause). No entry list is materialized and there is no per-document round-trip — **bounded work regardless of corpus size**, matching the precedent in `save_historical_tasks`. Verified by `corpora_test` (`post_corpora_extend_adds_new_entries` pins extend's only-new behaviour; the registration tests cover activate) + `services_test`, both green. Turned out cleaner/lower-risk than the initial "deferred" assessment once the diesel `INSERT…SELECT` precedent + the `UNIQUE` constraint made it a contained swap. `src/backend/services_aggregate.rs`. |
| E-3 | S4 | 🟢 | **`cortex export-dataset` held the whole work-list in RAM — fixed (2026-06-16, graduated from POSSIBLE_UPGRADES).** `export_html_dataset` loaded one severity's `entry` paths into a `Vec` and bucketed a lightweight `{result_zip, paper, yymm}` per task into a `BTreeMap` before bundling — O(corpus) resident (≈ a few hundred MB for the largest ~1.5M-paper corpus). Rewritten to **stream**: an `ArchiveStreamer` holds **at most one output archive open** at a time and copies each paper's HTML straight in, fed by a **keyset-paginated** entry scan (`EXPORT_PAGE_SIZE = 10_000`, `entry > $cursor ORDER BY entry` — `entry` is unique within a `(corpus, service)`, so it's a gap-free cursor, no deep-`OFFSET`). Both modes feed same-key entries contiguously: month mode does one cross-severity `ORDER BY entry` pass (each `yymm` is a contiguous run, so a month archive still aggregates papers from several severities); severity mode processes one severity at a time. Footprint is now **O(one page + one open zip + one paper's HTML)** regardless of corpus size. Resume parity preserved (an existing `.zip` is skipped without opening a writer). Contract pinned by `tests/export_test.rs` — the original single-paper test plus a new `streams_multiple_months_and_severities` (multi-`yymm` rotation + cross-severity month aggregation + per-severity spanning months). `src/backend/export.rs`. |
