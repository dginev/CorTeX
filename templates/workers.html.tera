{% extends "layout" %} {% block content %}
<div class="center">
  <h1>Workers for {{global.service_name}}</h1>
  <p class="fleet-summary">
    <strong>{{global.fleet_total | group_thousands}}</strong> workers
    <span class="report-sep">·</span>
    <span class="status-ok">{{global.fleet_fresh}} active</span>
    <span class="report-sep">·</span>
    <span class="muted">{{global.fleet_stale}} idle</span>
    <span class="report-sep">·</span>
    <span class="muted">{{global.fleet_dispatched}} dispatched / {{global.fleet_returned}} returned (lifetime)</span>
  </p>
  <p class="muted">
    <a href="/api/services/{{global.service_name}}/workers">view as JSON</a>
    <span class="report-sep">·</span>
    <a href="/runtimes/{{global.service_name_uri}}"><i class="fa fa-clock-o"></i> conversion runtimes &amp; slowest papers</a>
  </p>
  <br>
  <div>
    <table id="worker-report" class="table table-striped table-hover table-condensed">
      <thead>
        <tr>
          <th scope="col" class="center">Identity</th>
          <th scope="col" class="center" title="When the dispatcher last answered a work request from this worker — including empty-queue pings that lease no task. A live but idle worker stays current here even after the corpus is drained, so this tracks liveness, not work done.">Last Dispatch Requested</th>
          <th scope="col" class="center">Last Returned Time</th>
          <th scope="col" class="center">First Seen</th>
          <th scope="col" class="center" width="90px;">Total Tasks Dispatched</th>
          <th scope="col" class="center" width="90px;">Total Tasks Returned</th>
          <th scope="col" class="center" width="90px;" title="Tasks dispatched but not yet returned (dispatched − returned). A stale worker with a large outstanding count took work it never returned a result for.">Outstanding</th>
        </tr>
      </thead>
      <tbody>
        {% for worker in workers %}
        <tr class="{{worker.fresh}}">
          <td>{% if worker.name %}{{worker.name}}{% else %}<span class="muted">(unnamed)</span>{% endif %}</td>
          <td class="recent-time"><time datetime="{{worker.time_last_dispatch}}">{{worker.time_last_dispatch}}</time></td>
          <td class="recent-time"><time datetime="{{worker.time_last_return}}">{{worker.time_last_return}}</time></td>
          <td><time datetime="{{worker.first_seen}}">{{worker.first_seen}}</time></td>
          <td>{{ worker.total_dispatched | group_thousands }}</td>
          <td>{{ worker.total_returned | group_thousands }}</td>
          <td class="worker-outstanding{% if worker.outstanding != "0" %} has-outstanding{% endif %}">{{ worker.outstanding | group_thousands }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
{% endblock content %}